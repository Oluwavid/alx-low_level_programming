#include "main.h"
/**
 *_abs - main function of the program.
 *@n: integer variable for the function.
 *
 *Return: return values based on the condition.
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
